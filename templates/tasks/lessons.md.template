# Lessons Learned

<!-- Persistent cross-session learning journal.
     Format: ## YYYY-MM-DD — <Agent> — <Short title>
     Body: what went wrong, what the rule is going forward.
     NEVER delete entries — this is institutional memory. -->
